name: Temporary Weekday Weekend Formatter

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  format:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: "3.12"
          cache: pip
      - name: Install project
        run: python -m pip install -e ".[dev]"
      - name: Format targets
        run: ruff format reports/research/weekday-weekend-consistency/analysis.py tests/test_weekday_weekend_consistency_report.py
      - name: Upload formatted targets
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
        with:
          name: weekday-weekend-formatted-${{ github.sha }}
          path: |
            reports/research/weekday-weekend-consistency/analysis.py
            tests/test_weekday_weekend_consistency_report.py
          if-no-files-found: error
          retention-days: 1
