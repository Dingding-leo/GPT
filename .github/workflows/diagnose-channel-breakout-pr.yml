name: Diagnose Channel Breakout PR

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.13"
      - run: python -m pip install -e ".[dev]"
      - name: Capture full pytest output
        run: |
          set +e
          pytest -x -vv > /tmp/pytest-channel-breakout.log 2>&1
          status=$?
          set -e
          printf '%s\n' "$status" > /tmp/pytest-channel-breakout.status
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: channel-breakout-pytest-diagnostic
          path: |
            /tmp/pytest-channel-breakout.log
            /tmp/pytest-channel-breakout.status
          if-no-files-found: error
          retention-days: 1
