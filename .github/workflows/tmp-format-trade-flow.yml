name: Temporary Trade Flow Formatter

on:
  push:
    branches:
      - auto/research/20260728-23-trade-flow-schema-checkpoint

permissions:
  contents: write

jobs:
  format:
    if: ${{ !contains(github.event.head_commit.message, '[skip formatter]') }}
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.ref }}
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.12"
      - run: python -m pip install -e ".[dev]"
      - run: ruff check --fix scripts/acquire_okx_historical_trades.py scripts/audit_trade_flow_source_checkpoint.py scripts/stress_okx_trade_flow_checkpoint.py tests/test_okx_historical_trades.py
      - run: ruff format scripts/acquire_okx_historical_trades.py scripts/audit_trade_flow_source_checkpoint.py scripts/stress_okx_trade_flow_checkpoint.py tests/test_okx_historical_trades.py
      - shell: bash
        run: |
          set -euo pipefail
          rm -f .github/workflows/tmp-format-trade-flow.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "style: format trade-flow robustness checkpoint [skip formatter]"
          git push origin HEAD:${GITHUB_REF_NAME}
