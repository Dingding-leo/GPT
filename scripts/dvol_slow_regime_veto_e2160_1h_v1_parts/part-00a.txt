
# Transport-only repair: OKX history pages overlap enough that the theoretical
# ceil(rows / limit) budget can terminate before the requested start boundary.
# Preserve the frozen market interval and signal while allowing bounded overlap.
_ORIGINAL_FETCH_OKX_ONE_HOUR_CANDLES = fetch_okx_one_hour_candles


def _fetch_okx_one_hour_candles_with_overlap_budget(**kwargs: Any) -> Any:
    kwargs["safety_pages"] = max(int(kwargs.get("safety_pages", 0)), 64)
    return _ORIGINAL_FETCH_OKX_ONE_HOUR_CANDLES(**kwargs)


fetch_okx_one_hour_candles = _fetch_okx_one_hour_candles_with_overlap_budget

